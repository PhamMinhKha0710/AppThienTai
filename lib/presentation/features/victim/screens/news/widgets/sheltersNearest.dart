import 'package:flutter/cupertino.dart';

class SheltersNearest extends StatelessWidget {
  const SheltersNearest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
